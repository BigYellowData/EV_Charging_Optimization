# =============================================================================
# EV Charging Optimizer - Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration (Caltech ACN-Data)
# -----------------------------------------------------------------------------
CALTECH_API_KEY=your_api_key_here
CALTECH_API_URL=https://ev.caltech.edu/api/v1/sessions
CALTECH_SITE=caltech              # Options: caltech, jpl, office001
CALTECH_DATE=2019-07-15           # Date for real data (YYYY-MM-DD)
CALTECH_LIMIT=30                  # Max number of vehicles to fetch

# -----------------------------------------------------------------------------
# Optimization Parameters
# -----------------------------------------------------------------------------
N_VEHICLES=30                     # Number of vehicles (synthetic mode)
T_HORIZON=24                      # Time horizon in hours (1-168)
DT=1.0                            # Time step in hours

# -----------------------------------------------------------------------------
# Battery & Power Configuration
# -----------------------------------------------------------------------------
BATTERY_CAPACITY=30.0             # Default battery capacity in kWh
CHARGING_POWER_MIN=-6.0           # Min charging power (V2G) in kW
CHARGING_POWER_MAX=30.0           # Max charging power in kW
SITE_MAX_POWER=60.0               # Site transformer capacity in kW

# -----------------------------------------------------------------------------
# GDE3 Algorithm Parameters
# -----------------------------------------------------------------------------
GDE3_POP_SIZE=100                 # Population size (>= 10)
GDE3_N_GEN=1500                   # Number of generations (>= 100)
GDE3_VARIANT=DE/rand/1/bin        # DE variant
GDE3_CR=0.9                       # Crossover rate (0-1)
GDE3_F=0.5                        # Mutation factor (0-2)

# -----------------------------------------------------------------------------
# Infrastructure
# -----------------------------------------------------------------------------
CACHE_DIR=data_cache              # Cache directory path
CACHE_TTL=3600                    # Cache TTL in seconds
LOG_LEVEL=INFO                    # Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_FILE=                         # Optional log file path

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------
APP_NAME=EV Charging Optimizer
APP_VERSION=2.0.0
ENVIRONMENT=production            # development or production
